#ifndef MODULE_H
#define MODULE_H

/**
 * This is a function that searches the value of key in
 * array data. The array has size elements.
 * @param data array of integers
 * @param size number of elements in the array
 * @param key the value that the function is searching for
 * @return the position of key in the array, if key is not found it returns -1
 */
int LinearSearch(int data[], int size, int key);


#endif